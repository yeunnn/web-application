<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Appointments</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav>
        <div class="nav-content">
            <div class="title">Distributed DB App</div>
            <div class="nav-links">
                <a href="/appointments">View Appointments</a>
                <a href="/">Add Appointment</a>
            </div>
        </div>
    </nav>
    <% if (appointment) { %>
        <h1>Edit Appointment</h1>
        <form method="POST" action="/appointments/edit/<%= appointment.apptid %>">
            <input type="hidden" name="apptid" value="<%= appointment.apptid %>">
            <input type="date" name="date_of_appointment" value="<%= appointment.date_of_appointment.toISOString().slice(0, 10) %>" required>
            <select name="patient_gender" required>
                <option value="MALE" <%= appointment.patient_gender === 'MALE' ? 'selected' : '' %>>Male</option>
                <option value="FEMALE" <%= appointment.patient_gender === 'FEMALE' ? 'selected' : '' %>>Female</option>
            </select>
            <input type="number" name="patient_age" value="<%= appointment.patient_age %>" required>
            <input type="text" name="hospital_name" value="<%= appointment.hospital_name %>" required>
            <input type="text" name="doctor_specialty" value="<%= appointment.doctor_specialty %>" required>
            <input type="number" name="doctor_age" value="<%= appointment.doctor_age %>" required>
            <select name="region" required>
                <option value="Luzon" <%= appointment.region === 'Luzon' ? 'selected' : '' %>>Luzon</option>
                <option value="Visayas" <%= appointment.region === 'Visayas' ? 'selected' : '' %>>Visayas</option>
                <option value="Mindanao" <%= appointment.region === 'Mindanao' ? 'selected' : '' %>>Mindanao</option>
            </select>
            <button type="submit">Update Appointment</button>
        </form>
    <% } else { %>
        <p>Appointment not found.</p>
    <% } %>
</body>
</html>